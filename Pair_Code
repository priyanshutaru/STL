#include<bits/stdc++.h>
#include <iostream>
using namespace std;


int main() {
   
   pair<int,string> p;
   //p=make_pair(2,"abc");
  /*p = {2,"abc"};
  pair<int,string>&p1=p;
  //p1=p;
  p1.first=5;
  
  cout<<p.first <<" "<< p.second<<endl;

  int a[]={1,2,3};
  int b[]={4,5,6};
  pair<int,int>p_arr[3];
  p_arr[0]={1,2};
  p_arr[1]={3,4};
  p_arr[2]={5,6};
  swap(p_arr[0], p_arr[2]);
  for(int i=0;i<3;i++){
    cout<<p_arr[i].first<<" "<< p_arr[i].second<<endl;
  }*/
  cin>>p.first;
  cout<<p.first<<endl;
  cin>>p.second;
  cout<<p.second<<endl;
  

  
  
   //cout<<"Hello World Of Replit"<<endl;
   //cout<<"I am New On this Platform"<<endl;


  return 0;
}
